[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pokemon-helper"
version = "0.0.1"
description = "Windows overlay assistant for Pokemon emulators (Gen 1-5)"
readme = "README.md"
requires-python = ">=3.14"
license = { text = "MIT" }
authors = [{ name = "Flavio Paravisi" }]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "pre-commit>=4.0",
]
# Windows-only runtime deps added in later phases (F2+):
#   PySide6, windows-capture, rapidocr-onnxruntime, imagehash, pillow
app = []

[project.scripts]
pokemon-helper = "pokemon_helper.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/pokemon_helper"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"

[tool.coverage.run]
branch = true
source = ["pokemon_helper.engine", "pokemon_helper.data"]

[tool.coverage.report]
fail_under = 90
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
]

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
